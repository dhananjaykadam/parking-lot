# Add script to:
# * Install dependencies
# * Build/Compile
# * Run Test Suit to validate
#
# After this is run, bin/parking_lot
# should Just Work.

#if below npm i fails, give it a retry and it will work!
cd parking_lot && npm i && npm run test
